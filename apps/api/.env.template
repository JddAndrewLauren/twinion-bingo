# op:// references only — never a real value. Read with `op read` or hand the
# whole file to `op inject` / `op run --env-file=apps/api/.env.template`.
#
# The one shared secret /admin (#125) checks a request's Authorization: Bearer
# header against. One operator password, not an account (D11's "no accounts" is
# narrowed by this surface, not broken by it) — created once in the `dev` vault
# and referenced here, never committed as a literal value.
ADMIN_SECRET=op://dev/twinion-bingo-admin-secret/password
