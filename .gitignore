# Secrets — never commit
.env
.env.*
!.env.example

# Dependencies
node_modules/
.pnpm-store/

# Build output
.next/
dist/
build/
*.tsbuildinfo

# Regenerated by every `next dev` / `next build`, and the two disagree about the
# routes.d.ts path — tracking it means a permanently dirty tree.
apps/web/next-env.d.ts

# Visual gate output — reports and failure traces, regenerated by every run
test-results/
playwright-report/

# Platform
.vercel/
.fly/

# Editor / OS
.DS_Store
.idea/
*.swp

# to-goal batch sub-worktrees
.claude/worktrees/
